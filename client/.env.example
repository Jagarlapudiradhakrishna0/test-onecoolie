# Backend API endpoint (in production, use your deployed server URL e.g. https://api.yourdomain.com/api)
VITE_API_URL=https://your-api-domain.com/api

# Real-time WebSocket server endpoint (optional if same as API host without /api)
VITE_SOCKET_URL=https://your-api-domain.com
